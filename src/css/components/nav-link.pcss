@layer components {
  .nav-link {
    @apply p-2 rounded-md flex items-center transition-colors hover:bg-blue-700 hover:text-white;

    &.active {
      @apply bg-blue-700 text-white;
    }

    span {
      @apply ml-2;
    }

    svg {
      @apply h-5 w-5;
    }
  }
}
